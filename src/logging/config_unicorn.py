# # project
# from <core_module>.settings import settings

# bind: str = f"{settings.project_host}:{settings.project_port}"

# workers: int = settings.gunicorn_workers
# threads: int = settings.gunicorn_threads
# worker_class: str = settings.gunicorn_worker_class
# logconfig_dict: dict = settings.log_config

# loglevel: str = "info"
# accesslog: str = "-"  # stdout
# errorlog: str = "-"  # stderr
